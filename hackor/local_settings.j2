"""
Secret Django settings that should not be made public.

Jinja2 or Django template interpolating can be used to
create a real local_settings.py populated with your secret values.
"""

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = r'{{ django_secret_key }}'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '{{ db_name }}',
        'HOST': 'localhost',
        'PORT': '5432',
        'USER': '{{ db_user }}',
        'PASSWORD': '{{ db_password }}',
    }
}
